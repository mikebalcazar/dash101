# Un `_redirects` en la carpeta publicada manda sobre `netlify.toml`. La regla
# va aquí para que sea la que gane, y repetida en `netlify.toml` para que se
# vea. El `!` es el `force`: sin él Netlify sirve el archivo cuando existe
# —y `index.html` existe, dos renglones abajo— y la regla no se aplica nunca.
/*  https://dash101.mike-929.workers.dev/:splat  302!
